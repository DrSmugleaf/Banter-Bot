{
  "MYSQL_CONNECTION_LIMIT": {
    "description": "The maximum number of connections to create at once.",
    "default": 10
  },
  "MYSQL_DATABASE": {
    "description": "Name of the database to use for the connection."
  },
  "MYSQL_HOST": {
    "description": "The hostname of the database you are connecting to.",
    "default": "localhost"
  },
  "MYSQL_LOCAL_ADDRESS": {
    "description": "The source IP address to use for TCP connection.",
    "required": false
  },
  "MYSQL_PASSWORD": {
    "description": "The password of that MySQL user.",
    "required": false
  },
  "MYSQL_PORT": {
    "description": "The port number to connect to.",
    "default": 3306
  },
  "MYSQL_SOCKET_PATH": {
    "description": "The path to a unix domain socket to connect to. When used host and port are ignored.",
    "required": false
  },
  "MYSQL_USER": {
    "description": "The MySQL user to authenticate as."
  },
  "NODE_ENV": {
    "description": "Defines the current environment (set to dev in a development environment or leave empty).",
    "required": false,
    "validators": [{
      "name": "in",
      "options": ["dev"]
    }]
  },
  "PORT": {
    "description": "The port the application will use.",
    "default": 3000
  }
}
